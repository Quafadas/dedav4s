package build.experiments

import mill.*
import mill.scalalib.*
import mill.scalajslib.*
import mill.scalajslib.api.*

object `package` extends build.Common with build.ShareCompileResources:
  def moduleDeps = Seq(build.core.jvm)

  def mvnDeps = super.mvnDeps() ++ Seq(
    mvn"io.github.quafadas::scautable::0.0.34"
  )

  override def enableBsp = true
end `package`
